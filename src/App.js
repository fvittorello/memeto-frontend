import './App.css';
import { Header } from 'components/Header';
import { MainContainer } from 'components/MainContainer';

// { title, date, img, likes, comments, favs, tags }

function App() {
	return (
		<>
			<Header />
			<MainContainer />
		</>
	);
}

export default App;
